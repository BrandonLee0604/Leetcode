# Definition for a binary tree node.

class TreeNode:
     def __init__(self, val=0, left=None, right=None):
         self.val = val
         self.left = left
         self.right = right

class Solution:
    def isSymmetric(self, root: TreeNode) -> bool:
        while root.left != None
        return 0

root = TreeNode([1,2,2,3,4,4,3])
solution = Solution()
print(solution.isSymmetric(root))